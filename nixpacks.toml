# Nixpacks configuration for Railway
# Forces Node.js environment instead of Python auto-detection

[phases.setup]
nixPkgs = ["nodejs_20", "npm"]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "npm start"
